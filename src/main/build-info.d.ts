// Injected by electron.vite.config.ts's `define` — see there for how each is
// computed. Only `src/main/menu.ts` reads these.
declare const __GURT_COMMIT__: string
declare const __GURT_BUILD_DATE__: string
